block:
  description: Create a Coolify project to group services and applications
  runner: terraform
  layer: platform
  provider: coolify
  required_secrets:
    - COOLIFY_TOKEN

provides:
  coolify.project_uuid:
    type: string
  coolify.project_name:
    type: string

requires: {}
