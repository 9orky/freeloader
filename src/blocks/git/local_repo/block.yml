block:
  description: Initialises a local git repository and sets the remote origin.
  layer: source

costs:
  tier: always_free

provides: {}

requires: {}

config:
  basic:
    - name: remote_url
      description: Git remote URL to set as the origin.
      required: true

  advanced:
    - name: target_folder
      description: Project directory to initialise. Defaults to project.path.
